[ req ]
default_bits       = 384
prompt             = no
default_md         = sha384
distinguished_name = dn
req_extensions     = req_ext

[ dn ]
CN = node.quicap.local

[ req_ext ]
subjectAltName = @alt_names

[ alt_names ]
DNS.1 = node.quicap.local
